// acá seteo que cuando buildee la app para amplify, va a usar esta apiURL que me dio ApiGateway en AWS

export const environment = {
    production: true,
    apiUrl: 'https://pkuq7prfnd.execute-api.us-east-1.amazonaws.com/api'
}